#!/usr/bin/env bash
set -euo pipefail

for _ in {1..40}; do
    if qdbus6 org.kde.plasmashell /PlasmaShell >/dev/null 2>&1; then
        qdbus6 org.kde.plasmashell /PlasmaShell org.kde.PlasmaShell.evaluateScript \
            "$(cat /usr/local/share/membrane-os/recovery-desktop.js)"
        exit 0
    fi
    sleep 0.25
done

exit 1
